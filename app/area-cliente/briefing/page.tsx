import { BriefingForm } from "@/components/briefing-form"
export default async function PortalBriefingPage({ searchParams }: { searchParams: Promise<{ tipo?: string; id?: string }> }) { const { tipo, id } = await searchParams; const valid = tipo === "order" || tipo === "subscription"; return <section className="mx-auto max-w-4xl"><p className="text-sm font-medium text-[#155EEF]">Briefings</p><h1 className="mb-8 mt-1 text-3xl font-semibold">Novo briefing</h1>{valid && id ? <div className="rounded-xl border bg-[#07112d] p-6 text-white shadow-xl md:p-9"><BriefingForm endpoint="/api/portal/briefings" resource={{ type: tipo, id }} /></div> : <p>Pedido ou assinatura não encontrado.</p>}</section> }
